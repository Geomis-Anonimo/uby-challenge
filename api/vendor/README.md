# vendor

Integrações de terceiros com patch local. Não são gerenciadas pelo `package.json`
porque dependem de correções que nunca subiram para o upstream.

Ver `vendor/<pacote>/NOTAS.md` em cada diretório.
